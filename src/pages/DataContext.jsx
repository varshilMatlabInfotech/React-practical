import { useContext, useEffect } from 'react';
const DataProvider = useContext();
export default DataContext = ({ childrean }) => {
  useEffect(() => {}, []);
  return <DataProvider value={{}}>{childrean}</DataProvider>;
};
